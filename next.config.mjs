/** @type {import('next').NextInitialConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['localhost'],
    },
    // We'll add more context-specific config later if needed
};

export default nextConfig;
